/**
 * Multiplies two numbers
 * 
 * @param {number} x - the first number
 * @param {number} y - second num
 * @returns {number} Output is product of two numbers
 */
function multiply(x, y) {
    return x*y;
}

console.log(multiply(2, 3));